package tests;

import java.util.HashMap;
import java.util.Iterator;

public class staring1 {

	public static void main(String[] args) {
		
		String currentWindow	=driver.getwindowgandler();
	String<>	window	=	driver.getWindowhandlers()
			for (String string : window) {
			driver.setWindowhandler(string) {
				url	=	driver.getURL();
				if (url.contains("support")) {
					driver.setWindowhandler(currentWindow);
				}
			}
				
			}
		
		
		
		

	}

}
